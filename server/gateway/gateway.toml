# gateway conf

ver = "1.0.0"
logPath = "/tmp/gateway.log"

[server]
proto = "tcp"
addr = "0.0.0.0:10000"

[etcd]
root = "/server/access_server/"
addrs = ["localhost:2379"]
timeout = 1000

[serviceDiscovery]
role = "master"
interval = "5s"