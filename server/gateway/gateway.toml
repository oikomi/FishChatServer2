# gateway conf

ver = "1.0.0"
logPath = "/tmp/gateway.log"

[server]
proto = "tcp"
addr = "0.0.0.0:10000"

[serviceDiscovery]
role = "master"
interval = "10s"

[etcd]
root = "/server/access_server/"
addrs = ["172.16.245.128:2379"]
timeout = "1s"

