package protocol;

message ReqMsgServer {
    required uint32 cmd = 1;
    optional string cmdStr = 2;
}

message SelectMsgServerForClient {
    required uint32 cmd = 1;
    optional string cmdStr = 2;
    required string addr = 3;
}

