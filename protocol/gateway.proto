syntax = "proto3";

package protocol;

message ReqMsgServer {
    uint32 cmd = 1;
    string cmdStr = 2;
}

message SelectMsgServerForClient {
    uint32 cmd = 1;
    string cmdStr = 2;
    string addr = 3;
}

