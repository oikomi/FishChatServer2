syntax = "proto3";

package rpc;

service RouterRPC {
    rpc RouterAccess (RTAccessReq) returns (RTAccessRes) {}
}

message RTAccessReq {
    int64  UID       = 1;
}

message RTAccessRes {
    uint32 errCode  = 1;
    string errStr   = 2;
    int64  serverID = 3;
}